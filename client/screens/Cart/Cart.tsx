import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

function Cart() {
  return (
    <SafeAreaView>
      <Text>Carts</Text>
    </SafeAreaView>
  );
}

export default Cart;
